- name: Ensure Python 3 is installed
  ansible.builtin.package:
    name: python3
    state: present

- name: Ensure Pip for Python 3 is installed
  ansible.builtin.package:
    name: python3-pip
    state: present
